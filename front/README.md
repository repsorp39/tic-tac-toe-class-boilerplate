# 🎮 Tic Tac Toe — Frontend (React + Vite)

Frontend du jeu **Tic Tac Toe**, développé avec **React** et **Vite**, et connecté au backend Express.  
L’application permet de jouer une partie, d’enregistrer les coups et de récupérer l’historique depuis l’API.

---

## 🚀 Prérequis

- **Node.js** (version 18+ recommandée)
- **npm**
- Un backend fonctionnel et lancé sur `http://localhost:3500`  
  👉 Si ce n’est pas fait, suivez le README du backend.

---

## 📦 Installation

1. **Cloner le projet**

```bash
git clone https://github.com/repsorp39/tic-tac-toe-class-boilerplate tic-tac-toe
cd ./tic-tac-toe/front
npm install
```

Dans le fichier .env

```bash
cp .env.example .env
VITE_API_URL=http://localhost:3500
```

## Lancement
```bash
npm run dev
http://localhost:5173
```